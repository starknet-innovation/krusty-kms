# she-core-wasm

WASM bindings for shared cryptographic primitives from `she-core`.

## Build
Prereq: `wasm-pack` installed.

```sh
./build-wasm.sh
./build-wasm.sh --dev
```

Output is written to `pkg/`.
