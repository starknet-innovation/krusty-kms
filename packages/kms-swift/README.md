# GhoulKms (Swift)

SwiftPM wrapper over the `libkms` C ABI.

The package expects `libkms` in `zig/zig-out/lib` by default.

`Sources/GhoulKms/GhoulKms.swift` provides typed wrappers for the full `kms.h` API surface.
