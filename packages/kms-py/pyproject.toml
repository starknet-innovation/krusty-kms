[project]
name = "ghoul-kms"
version = "0.1.0"
description = "Python ctypes wrapper for Ghoul KMS"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Ghoul Contributors" }]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
