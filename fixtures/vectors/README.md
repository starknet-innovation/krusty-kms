# Shared KMS Vectors

These vectors are shared by core tests and all language wrappers.

Sources:
- Rust reference crate: `crates/core/kms`
- Cross-language parity vectors in Rust tests

All hex strings are lowercase and `0x`-prefixed unless explicitly byte-hex.
